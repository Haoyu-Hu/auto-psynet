---
command: setup
description: First-run Auto-PsyNet config — LLM-participant backend, username/server prefix, AWS creds, base domain, consent default.
allowed-tools: Bash, Read, Write, Edit, AskUserQuestion, Skill
---

Run the **`apsy:setup`** skill (invoke the Skill tool with skill `apsy:setup`) and follow its execution
contract to configure Auto-PsyNet. This is the recommended first command after install.
