[Parameters]
title = {{TITLE}}
description = {{DESCRIPTION}}
contact_email_on_error = {{CONTACT_EMAIL}}

# Most config (recruiter, wage_per_hour, base_payment, Prolific params) lives in experiment.py's
# `config` dict / get_prolific_settings(), matching the lab's example experiments. Recruiter-specific
# blocks can be added here or set via apsy:prolific / apsy:lucid / apsy:mturk.
